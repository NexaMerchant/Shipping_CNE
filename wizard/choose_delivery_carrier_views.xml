<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="choose_delivery_carrier_view_form_inherit_cne" model="ir.ui.view">
            <field name="name">choose.delivery.carrier.form.inherit.cne</field>
            <field name="model">choose.delivery.carrier</field>
            <field name="inherit_id" ref="delivery.choose_delivery_carrier_view_form"/>
            <field name="arch" type="xml">
                <form position="inside">
                    <group>
                        <field name="shipping_zip" invisible="1"/>
                        <field name="shipping_country_code" invisible="1"/>
                        <field name="is_cne" invisible="1"/>
                        <field name="cne_brand" invisible="1"/>
                        <field name="cne_colLivMod" invisible="1"/>
                        <field name="cne_allowed_countries" invisible="1"/>
                    </group>
                </form>
            </field>
        </record>
    </data>
</odoo>